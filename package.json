{
  "name": "spotcheck",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Dev tooling for the SpotCheck repo. The extension itself ships no dependencies and no build step - see extension/.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "test:watch": "node --test --watch \"tests/**/*.test.js\""
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
